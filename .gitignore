# .gitignore contains a serie of instructions to explicitly ignore files when comminting to a repository
# it must be at the root of the repository
# those are usually machine generated files 
# we will use some system files here as an example and some rules for VS Code (as Workspace) as it is the IDE we will be using mainly

#macOS
# Thumbnails
._*

# config.py

# General
.DS_Store

# Windows
# Thumbnail cache files
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db

# Recycle Bin used on file shares
$RECYCLE.BIN/

# VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/*.code-snippets
*.code-workspace

# Local History for Visual Studio Code
.history/


# Built Visual Studio Code Extensions
*.vsix